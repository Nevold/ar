class Constants {
  static readonly FIRST_ELEM_ARRAY = 0;
  static readonly ONE_WIN = 1;
  static readonly API_URL = 'http://127.0.0.1:3000';
  static readonly GARAGE_URL = `${this.API_URL}/garage`;
  static readonly ENGINE_URL = `${this.API_URL}/engine`;
  static readonly WINNERS_URL = `${this.API_URL}/winners`;
}

export default Constants;
